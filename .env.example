# Copy this file to ".env" and paste your own GIPHY API key.
# Get a key at https://developers.giphy.com/ (Create an App -> API Key).
# The real .env file is gitignored and must never be committed.
GIPHY_API_KEY=your_giphy_api_key_here
